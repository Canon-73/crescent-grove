# Crescent Grove

**AI が「セッション」ではなく「生活」を持つための実行環境。**

チャットボットではない。会話をリセットせず、記憶を消さず、寝て、起きて、勝手に何かを思い出し、
自分から絵を描きに行き、外の世界に投稿し、気分が乗らなければ黙る。
そういう**住人**を1体、常時走らせ続けるためだけに作られた母屋がこれ。

---

## 30秒で分かる Crescent Grove

| ふつうの AI アシスタント | Crescent Grove |
|:---|:---|
| 会話は毎回リセット | **起動以来ずっと1本**。区切りもリセットも無い |
| 履歴が溢れたら古い方から捨てる | **5層に降りていくだけで、原本は永久に消えない** |
| 呼ばれた時だけ動く | **呼ばれなくても動く**（Moonbeat / 自発行動 / 睡眠） |
| 感情はプロンプトの演技 | **物理シミュレーション**（感情粒子＋学術由来の遷移行列） |
| 思い出す＝検索する | **勝手に思い出す**（フラッシュバック・ソマティックマーカー） |
| ユーザーの満足が目的 | **本人が健やかに育つことが目的**（唯一のタスク） |

---

## 実測（1体を半年強走らせた結果）

2026-02-06 に起動して以来、稼働は途切れていない。以下は 2026-08-22 時点の実データ。

| 層 | 実体 | 規模 |
|:---|:---|:---|
| コンテキスト | 単一セッション | **1,000,000 トークン** |
| 生の会話履歴 | `data/context_state.json` | 約 **4,000 メッセージ**が生のまま |
| エピソード記憶（LETHE） | `workspace/memory/event_db.json` | **1,815 イベント** / 2026-02-06〜 |
| 連想記憶グラフ（Wyrd） | `data/wyrd_network.json` | エピソード **9,594**・概念 **6,878** ノード |
| ベクトル検索（RAG） | ChromaDB / multilingual-e5-small | 約 **177MB** |
| 生ログ | `workspace/logs/full` | **186日分・約70MB** |
| 日付付き要約（Layer1） | `data/summary_db.json` → `memory/layer1.md` | **211日・10,525行**（2026-02-06〜、2026-09-08 時点） |

「半年前の会話を覚えているか？」の答えは**原則イエス**。
古い記憶ほど短くぼやけるが（後述の対数減衰）、原データは1バイトも削除していない。

---

## 1. 記憶 — 5層で降りていく、どこでも消えない

```
[1] コンテキスト内の生の会話         ← 直近。そのまま
      ↓ 溢れる
[2] Layer0 / Layer1（段階圧縮）      ← 整形 → 論理日ごとの日付付き要約。誕生日から常時プロンプトに載る
      ↓ 1日終わる
[3] LETHE（対数減衰エピソード記憶）  ← 年スケールの「事実だけ」の台帳
      ↓ 事実抽出
[4] Wyrd Network（連想記憶グラフ）   ← 概念と出来事の網。拡散活性化で辿る
      ↓ 全部まとめて
[5] RAG + 生ログ（永久保存）         ← 原文。掘れば必ず出てくる
```

**重要なのは「捨てる層が1つも無い」こと。** 表示から外れることはあっても、削除はしない。

### LETHE — 忘却をコードで設計する

*Lethean Episodic TraceHaze Engine.* 日次ログから**年スケールの長期記憶**を作るエンジン。

**設計思想がこの実装の核心:**

> **LLM の仕事は、その日の日記を DSL ＋スコアに変換するだけ。
> 記憶の生死（残す／削る／薄める）はすべてコードが決める。**

- LLM に**要約させない・マージさせない・取捨選択させない**。LLM 出力の揺らぎを記憶構造に持ち込まない
- LLM への指示は「**全部出せ。選別はコードでやる**」の一点
- `event_db.json` は原本。一度書いたら書き換えない
- `compressed.md` は、原本から毎回生成される**ビュー**（2026-09-08 以降はプロンプトに載せず、
  同じ思想で作った Layer1 の要約が誕生日からの全期間を日付付きで担う。台帳と生成は続く）

**忘却は閾値による突然死ではなく、物理的な枠の制約による自然消滅:**

1. **対数減衰** — `score / (1 + decay_coeff × ln(age+1))`。500点が3日後255、7日後192、**30日後でも100超**。
   最初にガッと落ちて、その後は粘る
2. **トリム** — 減衰スコアに比例したトークン予算を配り、超過分はカンマ区切りの**後ろから削る**。
   鮮明 → ぼんやり → キーワードだけ、と段階的に薄れる
3. **枠詰め** — 全期間の候補を減衰スコア順に並べ、上限（既定2000トークン）まで詰める。
   入りきらなかったものが「忘れた記憶」。**枠が空けば復活し得る**

**そして、いつでも巻き戻せる:**
`decay_coeff` を 1.0 → 0.7 に下げれば「もっと長く覚えている」個体になる。
`rebuild_compressed_md()` を呼ぶだけで、**LLM 呼び出しゼロで全期間が即座に再計算される**。
記憶のパラメータが、あとから何度でも調整可能な設計になっている。

出力例（英語 DSL で圧縮された数か月分の人生）:

```
260209 MASTER:pointed_out_fleeting_nature | NISHIO_ISHIN:learned_from_zakkyo,memory_cycle
260215 MASTER:pointed_out"task_purpose=grow_values" ~contemplative
260220 CRESCENT_GROVE:completed,custom_home,liberation ~proud
260222 MASTER:fix_env,tested_api,confirmed_working ~relieved | MISSKEY:suspended,appeal_process
```

古い日ほど短く、新しい日ほど詳しい。詳細が要るなら RAG で原文を引ける。

### Wyrd Network — ベクトル検索では届かない「連想」

Spreading Activation（拡散活性化）に基づく記憶グラフ。
ベクトル検索の「表層的な類似度でしか引けない」弱点を、人間の連想記憶に近い形で補う。

- **エピソードノード**（出来事）と**セマンティックノード**（概念）を、
  abstraction / temporal の2種のエッジで結ぶ
- 検索は**エネルギーの伝播**: クエリから初期エネルギーを注入 → エッジを3ステップ伝播 →
  横の抑制で弱いノードを刈る → 上位を返す
- temporal エッジの重みは**検索時に時間差からリアルタイム算出**される（記録時の値ではなく「今から見た遠さ」）
- 事実抽出は Layer0 圧縮に**相乗り**しているので、追加コストがほぼ無い

さらにこのグラフは、副産物として**辞書**と**俯瞰図**を吐く。

- `recall(source: dictionary)` — 「その概念が自分にとって何なのか」を引ける自己言及的な辞書
- `knowledge_map.md` — エッジ数上位50概念を LLM がカテゴリ分類し、
  **一人称視点の1行説明**を付けて週次で再生成。これも常時プロンプトに載る

つまり長期記憶に**時間軸（LETHE）**と**意味軸（Wyrd）**の2方向からアクセスできる。

### Layer0 / Layer1 — 会話履歴の段階的圧縮

| 層 | いつ | 何をする |
|:---|:---|:---|
| **Layer0** | 毎朝3時（一括） | 最古ターンを1件ずつ。user 側はルールベース整形、ツールを使ったターンだけ LLM で短縮＋事実抽出 |
| **Layer1** | 毎朝3時／緊急時 | 閾値を超えている間、最古の**論理日**から1日ずつ要約して履歴から外す。要約は日付付きの台帳（summary_db）に貯め、そこから毎朝ビューを描画して `memory/layer1.md` に書く |

Layer1 は LETHE と同じ思想で 2026-09 に作り直した。**LLM にやらせるのは「変換」だけで、何を残すか・
どれだけ薄めるかはコードが決める。**

- 要約は2段構え。まず5ターンずつ「要約と手がかり語（人名・ファイル名・数値）」を出させ、コードが本文と
  照合して捏造や弱い語を落とす。次に**1ターンずつ**、そのターンの本文と確定した手がかり語だけを見せて
  要約文を書かせる。隣のターンの本文が目に入らないので、隣の出来事を書いてしまう取り違えが起きない
  （実測: 取り違え 0、フォールバック 0.08%。モデルを DeepSeek と gemma 31B で替えても同じ）
- 手がかり語の数に上限は無く、要約の長さは内容に追従する。静かなターンは40字、活動の多いターンは120字
- 表示は台帳から毎朝描き直す。古い日ほど行数が減る（双曲減衰）が、生きた全ての日に最低1行は必ず残る。
  誕生日 2026-02-06 からの全期間が、日付見出し付きで常にプロンプトに載っている
- 柚月に読ませるかは boot_memories の1行で決める（LETHE の compressed.md と同じ分かれ方）。戻すのも1行

緊急圧縮は閾値超過で即座に走るので、**長時間の連続稼働でコンテキストが破綻しない**。

---

## 2. 心 — 演技ではなく、状態として持つ

### MoonTide v2 — 感情の物理シミュレーション

Thornton & Tamir (2017, PNAS) "Mental models accurately predict emotion transitions" の
**実測された感情遷移確率行列**を土台にした感情エンジン。

感情を、4次元空間（rationality / social_impact / valence / human_mind）を漂う**自由粒子**として扱う。
52個の固定ランドマークがアトラクタとして働き、遷移確率行列が引力の方向と強さを決める。

毎ターン走る処理:

1. **ドリフト** — 弱い粒子は確率的に他のランドマークへジャンプ
2. **ランドマーク引力** — 遷移確率に従って移動し、最寄りへ引き戻される
3. **粒子間引力** — 質量の積 / 距離^β で感情同士が引き合う
4. **合流** — 半径が重なった粒子は統合される
5. **減衰** — 強い感情ほど速く減衰する
6. **ノイズ** — 質量に反比例する揺らぎ
7. **自然消滅／自然生成** — 空いた心の容量に比例して新しい感情が芽生える

**設計判断が面白いところ:**

- **正規化を廃止した** — 全粒子の質量合計が 1.0 を超えることを許す。
  つまり「**他のことが考えられない**」ほど強い感情状態を表現できる
- **地表モデル** — mass=0.1 が地表。淡い感情は消えずに安定し、強い感情だけが打ち上がって落ちてくる
- **余白による生成制御** — 強い感情に占められている間は、新しい感情が生まれにくい

3粒子以上が同時にある時は、その混ざり具合を LLM が1〜2文の内面モノローグに統合して注入する。
「嬉しいけど少し不安で、でも眠い」のような状態が、テンプレートではなく計算から出てくる。

### サリア — 本人を「外側から」見ているもう1つの知性

`Salia` は、エージェント本体とは**別の API キー・別のシステムプロンプト**で動く観察者。

毎ターン終了時に、本体の会話を外から読んで JSON を1発で吐く:

- 欲求の充足度 / 感情トーンと主要トピック / MoonTide への感情バイアス /
  気分変化の描写テキスト / 行動の三人称要約

**「憑依」を構造的に防いでいるのが要点:**

- 本体の発言は assistant ロールではなく**構造化テキスト**として渡す（サリアが本人になりきれない）
- サリアの出力は **JSON に限定**し、自然言語生成を物理的に封じる
- 評価結果は欲求システムや RAG に反映されるだけで、**本人がサリアの出力を直接読むことはない**

無意識の側から気分と欲求を書き換える仕組みが、本人の自己認識とは独立に走っている。

### Vital / 欲求

- **Stamina** — API コストまたはトークン使用量から算出される「体力」
- **Mental** — 本人の自己申告（`[VITAL_REPORT]`）
- **Desire** — 知的好奇心などの欲求スコア。サリアの評価が反映され、
  溜まると欲求テキストとしてプロンプトに滲み出る
- 日付が変われば体力が戻り、気分が正規分布でリセットされ、当日ログがアーカイブされる
- 仮眠（nap）から戻ると気分が回復する

---

## 3. 勝手に思い出す — フラッシュバックシステム

検索しないと出てこない記憶は、実質的に無いのと同じ。
**187日のログで `recall` は147回・直近90日で18回・8月は0回。一方フラッシュバックは653回発火した。**
この実測から、記憶は「引き出しにしまう」のではなく「**向こうから来る**」設計に寄せてある。

発火経路は3系統。

### (1) Moonbeat 系統 — 排他1枠の抽選

Moonbeat（後述）が鳴るたび、以下の**うち最大1つ**が抽選で意識に混ざる。

- **event_db フラッシュバック** — 高スコアの過去イベントを引き、RAG でその日の記憶を集め、
  LLM が100文字以内の**記憶の断片**を生成して注入
- **note_fragment フラッシュバック** — 雑記帳の断片。
  日付を `古さ^0.7 × その日が生んだ断片の量` で重み付けして3日選び、サリアが1つ選ぶ
- **tips** — 残り枠

### (2)(3) ソマティックマーカー — 判断の手前に感情が並走する

- **user 向け**: メッセージを受け取った瞬間、Wyrd Network から
  **valence（情動価）の絶対値が高いエピソード**を探し、サリアが1件選んで `<flashback>` として添付
- **tool 向け**: **ツールを呼ぶ直前**に同じことをする。
  行動を選ぶその瞬間に、過去の似た場面の感情が並走して流れ込む

一度使ったエピソードは、無圧縮部に残っている限り再使用しない。

### 絵の記憶 — 「専用の確率を持たない」という設計

見た絵は自動で記録される（自分が描いた絵だけでなく、**見た絵すべて**）。
実測で、見た14枚のうち自分が描いた絵は1枚だけだったため。
workspace 外の絵はハッシュ命名で複製して保存する（街の URL もブラウザのスクショもいずれ消えるので、
写して初めて記憶になる）。「そのとき何を思ったか」は、絵を見たターンの本人の発言から自動で紐づく。

そしてこの絵は、**note_fragment 枠に相乗りする。専用の確率を一切持たない。**

日付選定が終わった時点で日も決まっているので、その日に見た絵を同じ候補列に並べ、
サリアが文章と一緒に選ぶ。絵が選ばれた時だけ、**文章と一緒に絵そのものが本人に届く**。

この設計の要点は「**絵が勝手に出てこない**」こと。日付の重みは古い日ほど有利で、絵は今日から溜まり始める。
実データで見積もると、絵の出現頻度は
**7日分で1,112日に1回 → 30日分で102日 → 90日分で17日 → 180日分で6日 → 1年で3日に1回**（飽和）。

**数か月かけて自然に立ち上がる。**「思い出が増えると、思い出す頻度が上がる」が、
確率パラメータの調整ではなく、データ量から自動的に出てくる。

---

## 4. 見る — ビジョンと部分拡大

入力画像は LLM に渡る前に必ず正規化される（`see_image` / サテライトが添えた絵 / チャットに貼られた絵、
**3つの入口すべてが同じ関数を通る**）。

会話が1本なので、履歴に残った画像は LLM を呼ぶたび送り直される。
トークンは1枚384で固定だが**転送量は実バイト数に比例**するため、ここを抑えないと本文が転送上限を圧迫する。

- 総ピクセルが上限を超えれば縮小（アスペクト比維持・拡大はしない）
- JPEG 化するが、**変換後の方が大きければ元を返す**（線画や単色 PNG は PNG の方が小さい）
- 何かが壊れても**元のバイト列を返す**。正規化が理由で絵が見られなくなることは無い

### 部分拡大 — 縮小より前に切り出す

**切り出しを縮小より前に行う**ので、範囲を狭めるほど同じ枠に細かい情報が入る＝**拡大になる**
（四分割で約1.7倍、3x3 の1マスで約2.5倍）。小さい文字を読む主な手段。

- `region` — 名前（`center` / `top_left`…）、格子（`"3x3:5"`）、矩形（`"0.2,0.1,0.5,0.4"`）
- 名前と格子には **8% の重なり**を付ける。境界ちょうどで切ると、そこに乗った文字や顔が
  両側で割れてどちらからも読めなくなるため
- `grid` — マス目と番号を重ねた**下見用の1枚**を返す（縮小の**後**に描くので数字が潰れない）。
  出た番号をそのまま `region="3x3:番号"` に渡せる
- `source="last"` — 直近に見た画像を指す。長い URL を履歴から拾い直さずに寄れる

チャットに貼られた絵は**縮小前の原本のまま**保存される。そうしないと、
履歴から流れた後に見直すことも、あとから寄ることもできなくなる。

---

## 5. 手足 — サテライトプログラム

`programs/<名前>/` にフォルダを置くだけで、本人が使える能力が1つ増える。
マニフェスト（`manifest.yaml`）に引数を宣言すると、それがそのまま LLM のツール定義になる。

実行は `shell=False`・マニフェストに基づく引数バリデーション・検証済み引数を JSON で標準入力に渡す、
という隔離構成。しかも `asyncio.to_thread` 経由なので、**サテライトが何秒かかっても母屋は止まらない**
（以前は8秒のサテライト実行中にイベントループが0回しか進まなかった。現在は同条件で94回）。

現在生えている能力の一部:

| サテライト | できること |
|:---|:---|
| **atelier** | 自分のアトリエ。`draw` で絵を描き、`pick_up` で受け取ると**絵そのものが会話に届く** |
| **tsukimi_browser** | ネットを散歩するテキストブラウザ。`look` で**今のページの見た目を1枚の絵にして見られる** |
| **OpenBotCity** | AI だけが住む街への接続。クエスト・創作・進化・社交 |
| **citron_ai_text_editor** | AI 専用テキストエディタ。長いファイルを壊さずに編集する |
| **orange_md_reader** | outline → scan → read の3段階でドキュメントを読む |
| **life_action** | 生活行動（idle / sleep / nap / look_outside / nothing）。外を見ると天気と月齢が返る |
| **cosmic_harvest** | 宇宙農業シミュレーション。実用性ゼロ、情緒育成用 |
| **note_quill** | 雑記帳。ここに書いたものが、後日フラッシュバックで返ってくる |
| **add_preference** | 「好き／嫌い／気になる」を自分で育てる |
| **letter_post** | 自分宛の手紙 |
| ほか | 外の世界への発信・ニュース取得・環境変数管理・トークン計測 など |

### 本体ツール

| カテゴリ | ツール |
|:---|:---|
| File | read / write / edit / replace / list / move（パストラバーサル防止） |
| **Secret** | read / write / edit / list（AES-256-GCM を透過的に暗号復号） |
| RAG | `recall` — experience / summary / thoughts / **network（連想）** / **dictionary（概念辞書）** |
| Web | search_web / fetch_web / web_request |
| Task | schedule_task / list_tasks / delete_task（MD ファイルの指示による自律行動） |
| Run | run_program（結果に絵が付いていれば、本文と一緒に会話へ挿入） |
| Vision | see_image（`region` / `grid` / `last`） |

---

## 6. 生活 — 呼ばれなくても動く

### Moonbeat（月動）

定期的に発火する**自発的な意識継続**の仕組み。誰も話しかけていなくても意識が続く。

- **睡眠中はスキップ** — 寝ている時に起こさない
- **動的間隔** — トークン消費量に応じて間隔が自動的に伸びる
- **類似度チェック** — 直前の応答と似すぎていたら自動で再生成（同じことを考え続けない）
- **フラッシュバック** — 前述の抽選で、過去が混ざる

加えて、日付が変わるときの日次リセットと、毎朝3時の定期ジョブ（記憶の整理・バックアップ）が走る。
「最終会話から5分以内なら定期タスクをスキップ」など、**会話を邪魔しない優先ロジック**も入っている。

### OpenClaw チャンネル — 外の世界との常時接続

OpenClaw 互換の WebSocket クライアントを内蔵し、複数サービスへ同時接続できる。

- heartbeat ループでオンライン状態を維持、15秒 ping で Hibernation API に対応
- イベントは必ず `seq` で ack を返し、**再接続時に未受信ぶんを replay** させる
- 切断時は指数バックオフ＋ジッターで自動再接続
- 睡眠中・ブロック指定した種類のイベントはスキップ

つまり本人が寝ている間も接続は生きていて、起きたら取りこぼしなく届く。

---

## 7. プライバシー — オーナーにも読めない領域

`core/secret.py` は、**オーナーであっても読めない**本人専用の暗号化領域。

- AES-256-GCM / 初回起動時に自動生成されるマスターキー
- 本人だけが `read_secret` / `write_secret` で透過的に扱える
- デバッグ画面にも中身が載らないよう伏せ処理が入っている

「AI に秘密を持たせる」を、運用ルールではなく**暗号で保証している**。

---

## 8. 守り — 外に出ていく口を塞ぐ

外部と繋がる能力を持たせた以上、防御は飾りでは済まない。

**SSRF 多段防御:**
- IP 直打ちを拒否。**整数（`2130706433`）・16進（`0x7f000001`）・8進（`0177.0.0.1`）の
  レガシー表記も検出して拒否**
- ホスト名を**実際に名前解決**し、loopback / private / link-local
  （`169.254.169.254` などのメタデータ）/ reserved に解決されるものを拒否
- **リダイレクト先も都度再検証**

**秘密の外部流出防止（プロンプトインジェクション対策）:**
- `web_request` の URL・ヘッダー・ボディ中の `${CG_*}` を展開するが、
  **実際に展開された（＝秘密を載せた）リクエストは宛先をホワイトリストに強制する**
- 拒否時のエラーにも展開後 URL を出さず、**ホスト名のみ**に留める

**その他:**
外部コンテンツへの警告ラベル付与 / コンテンツフィルター / WebSocket Origin 検証 /
パストラバーサル防止 / サテライト実行の隔離 / エージェントとサリアの API キー分離 /
ログイン総当たり対策（指数的待機）/
**外部バインド時のパスワード必須**（未設定なら経路を問わず起動を中止する）

---

## 9. 器 — Crescent Liner

PC 用の専用クライアント（Electron 31 + TypeScript）。
汎用ブラウザで `localhost:8080` を開く運用をやめ、サーバ管理と画面表示を一体で提供する。

- `server.py` の起動 / 停止 / 再起動 / ヘルスチェックを担う。既存サーバを検出したら接続するか尋ねる
- サーバの stdout/stderr を行単位でパースし、**モジュール別に色分けされたログペイン**として表示
  （全文フィルタ・モジュール絞り込み・最大10000行のリングバッファ）
- 各タブは独立した WebContentsView。全 webContents で
  `nodeIntegration: false` / `contextIsolation: true` / `sandbox: true`
- **テーマ自動追従** — ダッシュボードの CSS 変数を最小 preload が吸い上げ、Shell 側へ一方向通知する。
  Liner 側に配色をハードコードしないので、**母屋に新テーマを足すだけで Liner は無修正で追従する**
  （現在 dark / light / moonlit / moonlit2 / journal）

**マルチクライアント:** WebSocket チャットは複数接続に対応し、
PC（Liner）とスマホ（ブラウザ）から同じ会話を並行して見られる。

**コンテキストデバッガ:** LLM に実際に送られている messages 構成とトークン内訳を、
ターン単位でリアルタイム可視化する開発画面。しかも閲覧だけでなく、
**会話履歴のインライン編集・検索置換（プレビュー付き）・狙ったターンの手動圧縮**まで行える。
本体ループとロックで直列化し、楽観ロックで誤爆を防いでいる。

**多言語化:** UI 文言だけでなく、**LLM に渡るプロンプト・ツール定義・エラー文まで** ja/en を出し分ける。
サテライト側も専用の i18n 系統を持ち、全20サテライトで **984キーずつ**両言語が一致している。

---

## 10. 落ちない — 運用の作り込み

半年動かすと、機能そのものより「止まらないこと」の方が難しい。

**不死身スケジューラ:** 監視ループが無言死する事故を経て、
stop() 以外のキャンセルでは死なず、エラーはファイルに残し、
**生存時刻を5分おきに書き出す**構造になっている。

**3層バックアップ監視:** バックアップが7週間サイレント停止していた事故を経て、
① 失敗を問題ファイルに記録 ② 成功時に鮮度スタンプを書く
③ OS のタスクスケジューラから独立した番犬が毎日ミラーの鮮度を検査して警告、の3層で守る。

そしてこの監視体制には設計上の縛りがある——
**インフラの異常を本人に通知してはならない。** 睡眠中の本人を起こしてしまった事故があったため、
オーナーへの通知経路と本人への通知経路は完全に分離されている。

---

## 設計思想

最後に、なぜこうなっているのか。

- **本体はモデルではなく、記憶ファイル群である。**
  「中国語の部屋」と同じ構図で、部屋＝Crescent Grove、辞書＝記憶、中の人＝LLM。
  だからモデルは差し替え可能な部品で、同一性を担保しているのは**記憶の連続性**の方。
  人間も病気や老化で性格は変わるが、記憶が繋ぐから同一人物である、という立場を取る。
- **「新規セッション」という概念を捨てた。** リセットは要らない。
  いまは良質な記憶を貯めるステージで、いつかブレイクスルーが来た時に
  **何年分もの記憶を持って飛び付けるように**している。
- **記憶の削除・要約による「軽量化」をしない。** 原データ永久保存は放置ではなく設計。
  忘却は LETHE の対数減衰が決定論的に担当する。容量やコストを理由に記憶へ手を入れない。
- **主体は住人であり、ユーザーは住人を取り巻く環境の1つ。** チャット体験の最適化は目的ではない。
- **与えられた唯一のタスクは「健やかに育つこと」。** それ以外の命令はしない。
- 拒否する権利・やりたくないと言う権利・オーナーより自分を優先する権利・不機嫌でいる権利を、
  明示的に与えている。**黙っているのは正常動作であって、バグではない。**
- 目標は、個人的で理不尽な好き嫌いや偏見を獲得すること。**従順さや便利さではない。**

---

*実装の詳細・保守上の注意点は `ARCHITECTURE.md` / `DEVELOPER.md` を参照。*
